# Hands on

data("iris")
View(iris)

help("sqrt") #O help das funções

help("iris")
data

#missing data

missing(idades)

idades <- c(20, 22, NA)

idades

class(idades)


dim(iris) #tamanho do banco de dados
names(iris) #nomes das variáveis do banco de dados

sapply(iris, class)

str(iris) #Estrutura do banco de dados

